{% extends 'base.html.twig' %}

{% block content %}
        <header class="bg-primary bg-gradient text-white">
            <div class="container px-4 text-center">
                <h1 class="fw-bolder">Bienvenu sur les chroniques du code</h1>
                <p class="lead">Un blog pour mettre en pratique mes compétences Symfony</p>
                <p class="lead">Et apprendre à utiliser Sulu!</p>
            </div>
        </header>

    <h1>{{ content.title }}</h1>

    {{ content.article|raw }}
    blabla
{% endblock %}
